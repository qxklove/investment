spring:
  application:
    name: index-config-server
  cloud:
    config:
      label: master
      server:
        git:
          uri: https://github.com/qxklove/trend_investment/
          searchPaths: config
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
